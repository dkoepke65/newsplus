services:
  - type: web
    name: newsplus
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000